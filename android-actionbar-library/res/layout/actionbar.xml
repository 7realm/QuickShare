<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/actionbarLayout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/actionbar_height"
    android:descendantFocusability="beforeDescendants"
    android:focusableInTouchMode="true" >

    <RelativeLayout
        android:id="@+id/actionbarUpLayout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:background="@color/actionbar_separator"
        android:visibility="gone" >

        <ImageButton
            android:id="@+id/actionbarUpButton"
            style="@style/ActionBarHomeItem"
            android:background="@drawable/actionbar_button" />

        <ImageView
            android:id="@+id/actionbarUpIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentTop="true"
            android:background="@drawable/actionbar_back_indicator"
            android:padding="0dip"
            android:visibility="gone" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/actionbarActionList"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:orientation="horizontal"
        android:layout_margin="0dp"
        android:background="@color/actionbar_separator"
        android:padding="0dp" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@id/actionbarActionList"
        android:layout_toRightOf="@id/actionbarUpLayout" >

        <ProgressBar
            android:id="@+id/actionbarProgressBar"
            style="@style/ActionBarProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:indeterminateOnly="true"
            android:paddingRight="7dip"
            android:visibility="gone" />

        <com.markupartist.android.widget.ScrollingTextView
            android:id="@+id/actionbarTitleLabel"
            style="@style/ActionBarTitleView"
            android:background="@null"
            android:ellipsize="marquee"
            android:lines="1"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true" />

        <EditText
            android:id="@+id/actionbarTitleEdit"
            style="@style/ActionBarTitleView"
            android:background="@drawable/shape_edit_light"
            android:inputType="text"
            android:maxLines="1"
            android:visibility="gone" />

        <Spinner
            android:id="@+id/actionbarTitleSpinner"
            style="@style/ActionBarTitleView"
            android:background="@drawable/shape_edit_light"
            android:visibility="gone" />
    </RelativeLayout>

</RelativeLayout>